[ElasticSearchOutput]
server = "<%= @url -%>"
encoder = "<%= @encoder -%>"
message_matcher = "Logger != 'hekad' && Type != 'heka.statmetric'"

[<%= @encoder -%>]
type_name = "%{Type}"
index = "logs-%{2006.01.02}"
es_index_from_timestamp = true
